--- JAVA ---
cd \backend 
mvn clean install 
mvn spring-boot:run


--- PYTHON ---
cd \backend-python
py -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000

(Se não funcionar): python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000

--- FRONT-END ---
cd \frontend
py -m http.server 5500

http://localhost:5500 (ou http://127.0.0.1:5500)